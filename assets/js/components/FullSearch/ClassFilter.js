import React
    from "react";

const ClassFilter = ({params, setParams, open}) => {


    if (!open) return <></>
    return <section id={'classFilter'}>Class</section>
}

export default ClassFilter
