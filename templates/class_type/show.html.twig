{% extends 'base.html.twig' %}

{% block title %}ClassType{% endblock %}

{% block body %}
    <h1>ClassType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ class_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ class_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('class_type_index') }}">back to list</a>

    <a href="{{ path('class_type_edit', {'id': class_type.id}) }}">edit</a>

    {{ include('class_type/_delete_form.html.twig') }}
{% endblock %}
