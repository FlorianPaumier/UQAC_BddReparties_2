{% extends 'base.html.twig' %}

{% block title %}Beast{% endblock %}

{% block body %}
    <h1>Beast</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ beast.id }}</td>
            </tr>
            <tr>
                <th>Xp</th>
                <td>{{ beast.xp }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ beast.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ beast.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('beast_index') }}">back to list</a>

    <a href="{{ path('beast_edit', {'id': beast.id}) }}">edit</a>

    {{ include('beast/_delete_form.html.twig') }}
{% endblock %}
