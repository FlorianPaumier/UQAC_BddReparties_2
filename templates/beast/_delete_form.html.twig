<form method="post" action="{{ path('beast_delete', {'id': beast.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ beast.id) }}">
    <button class="btn">Delete</button>
</form>
